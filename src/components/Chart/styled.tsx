import styled from "styled-components";
import { TabWrapper } from "../../styles/Antd/Tab/styled";

export const ChartWrapper = styled.section``;

export const Tab = styled(TabWrapper)`
  margin-top: 28px;
  text-align: right;
`;
